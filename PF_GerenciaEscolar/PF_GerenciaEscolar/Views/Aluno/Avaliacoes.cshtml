@using PF_GerenciaEscolar.Data.Enum
@model List<Avaliacao>

<!doctype html>
<html lang="pt-br">
<body>
    <main role="main">

        <div class="album py-5">
            <section class="jumbotron text-center">
                <div class="container">
                    <h1 class="jumbotron-heading">Avaliações</h1>
                    <a asp-controller="home" asp-action="Index" class="btn btn-secondary my-2">Voltar</a>
                </div>
            </section>
            <div class="container">
                @if (Model != null)
                {
                    <div class="row">
                        @foreach (var avaliacao in Model)
                        {
                            <div class="col-md-4">
                                <div class="card mb-4 box-shadow">
                                    <div class="card-body">
                                        <p class="card-text">@avaliacao.Titulo - @avaliacao.Valor</p>
                                        <div class="d-flex justify-content-between align-items-center">
                                            <div class="btn-group">
                                                <a asp-controller="aluno" asp-action="DetalhesAvaliacao" asp-route-id="@avaliacao.Id" type="button" class="btn btn-sm btn-outline-secondary">Ver</a>
                                                <a asp-controller="aluno" asp-action="EnviarAvaliacao" asp-route-id="@avaliacao.Id" type="button" class="btn btn-sm btn-outline-secondary">Enviar</a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                }
            </div>
        </div>
    </main>
</body>
</html>


<style>
    body {
        margin: 0;
        padding: 0;
        font-family: sans-serif;
        background: linear-gradient(to right, #ccffff, #1565c0)
    }
</style></h1>